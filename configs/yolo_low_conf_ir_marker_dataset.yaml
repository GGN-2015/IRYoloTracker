# Low-confidence YOLO pseudo dataset generated by example_infrared_video.py.
# Labels come from the current inference model, not human annotation.

path: datasets/yolo_low_conf_ir_marker_ball
train: images/train
val: images/val
test:

names:
  0: ir_marker_ball
